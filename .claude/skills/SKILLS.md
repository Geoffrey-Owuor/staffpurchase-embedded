- For the shared filter logic used accross the tables in this repo, move towards a commited filters approach UI where we have a custom drop-down for selecting a filter type, selecting that filter type displays a search input or another dropdown to set a seach query or a value, the an apply filters button which applies the selected filters. Show axctive filter pills (still within the rounded card holding filter panel logic - active filter pills (rounded full) - should show after a value is selected or typed and change accordingly, not after filters are commited). Also a reset button for resetting filters. Apply this logic to the shared current filter logic accross all tables as you see fit. Current filter logic re-fetches on any value change which is undesired - New filter logic which should use a zustand store where data is fetched after commiting filters will be a better approach. Improve the filter panel UI (still using the current theming) while you are at it i.e. rounded buttons in drop-downs with the main drop-down also having a padding...and other UI improvements as you see fit. The filter panel (which also will house active filter pills) - should be sticky positioned when a user scrolls up.
- Remove the purchases history tab and route (I had already started this but did not finish in some places) from all sidebars and mobile headers and wherever the /dashboard/history was used and all it's referenced functions and helpers (This is because what was displayed in that route was the exact same thing displayed in the home route) generaledit and generalview pucrhases should now point to /dashboard/[id] route instead.
- Inspect the shared params filter builder which passes params to the card counts and table data queries, when I ran a test, it seemed like the passed params for the table queries was generating syntax errors and no data was being returned at all, even when filters were applied.
- Ask follow-up questions if you need further clarification.
- After making these changes, update README.md and CLAUDE.md where necessary.
